This is just an example .env file for setting environment variables.
# Copy this file to .env and modify the values as needed.

# Default: use external Postgres running on host (for tools like pgAdmin4)
DB_HOST=host.docker.internal  # container sẽ thấy Postgres local thông qua host.docker.internal
DB_PORT=5432
DB_NAME=CustomerChurn         # sẽ được script tự tạo nếu chưa tồn tại
DB_USER=postgres
DB_PASSWORD=your_password_here

# Nếu muốn chuyển sang dùng Postgres nội bộ trong docker-compose (service: postgres_airflow), có thể dùng:
# DB_HOST=postgres_airflow
# DB_PORT=5432
# DB_NAME=airflow
# DB_USER=airflow
# DB_PASSWORD=airflow